import {requestSession} from '@/server/auth';import {requestContext} from '@/server/audit';import {walkIn} from '@/server/intake-service';import {checkOrigin,readJson,json,errorResponse} from '@/server/http';export async function POST(request:Request){try{checkOrigin(request);const {user}=await requestSession(request,'appointment:create');return json(await walkIn(user,await readJson(request),requestContext(request)),201);}catch(e){return errorResponse(e);}}
